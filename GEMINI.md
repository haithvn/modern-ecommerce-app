## Gemini Added Memories
- The user is working on the 'melodyvn-event-talks-app' project (Node.js/Express backend, vanilla JS frontend). The server is running on http://localhost:3000. Local Git is initialized with an initial commit, and the remote GitHub repository name is intended to be 'melodyvn-event-talks-app' under the 'haithvn' account.
- Khi hoàn thành tính năng, luôn thực hiện merge các thay đổi vào nhánh 'master'.
- User is working on 'modern-ecommerce-app' (https://github.com/haithvn/modern-ecommerce-app). Stack: Spring Boot 3 (Java 17 via mvnw), H2 DB, React Vite (Node 22), TypeScript. Features: Multi-language (i18n), Multi-currency.
- Project 'modern-ecommerce-app' status: Backend Registration & Email Verification is implemented (merged in PR #4). Next task is Backend Login (JWT). Workflow established: 1. Create Issue -> 2. Create Branch -> 3. TDD (Write failing test first) -> 4. Implement Code -> 5. Verify Test (Pass) -> 6. Push & PR -> 7. Merge. Tech stack: Spring Boot 3, Security, JWT, JavaMailSender, React Vite.
- Project 'modern-ecommerce-app' status update: Completed Registration, Email Verification (Issue #5), Backend Login (Issue #7), and Frontend Login (Issue #9). Fixed CORS and Product access issues (Issue #10). Current stack: Spring Boot 3, React Vite (port 3000), JWT Auth. All changes merged into 'main'. Next steps: Shopping Cart or Admin features.
- Project 'modern-ecommerce-app' status update: Completed Shopping Cart functionality (Issue #13). Implemented CartItem entity, service, controller on Backend. Implemented Cart page, cartApi, and Add to Cart feature on Frontend. All changes merged into 'main'. Next potential task: Order placement or Product Search/Filtering.
- Workflow rule updated: After implementing a feature and pushing to a separate branch, wait for the user to verify the changes on their local server. Only merge the branch into 'main' after the user gives explicit approval ("ok").
